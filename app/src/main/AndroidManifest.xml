<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <!-- Разрешения -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!--это типо разрешение для андроид меньше 10-->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" /> <!--это типо разрешение для андроид больше 10-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!--хз нахрена-->
    <uses-permission android:name="android.permission.INTERNET" /> <!--хз нахрена-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Book"
        tools:targetApi="31"
        android:name=".BookApplication"
        android:requestLegacyExternalStorage="true"><!--вот эту фигню обязательно-->
        <activity
            android:name=".Home"
            android:exported="false" />
        <activity
            android:name=".Registration"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>